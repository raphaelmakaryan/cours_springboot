CREATE TABLE `LocationVehicule`.`Client`
(
    `ID`        INT          NOT NULL AUTO_INCREMENT,
    `idAPI`     INT          NOT NULL,
    `firstName` VARCHAR(100) NOT NULL,
    `lastName`  TEXT         NOT NULL,
    `birthday`  VARCHAR(100) NOT NULL,
    `license`   VARCHAR(9)   NOT NULL,
    PRIMARY KEY (`ID`)
);